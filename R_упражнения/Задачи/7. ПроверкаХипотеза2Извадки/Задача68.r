# Виж задача 86 от problems_v8.pdf

# Тази задча се решава с z-тест за разлика на пропорция

x <- c(26, 43)
n <- c(500, 540)

# p1 е вероятността някой да е вегетараианец през 2007
# p2 e вероятността някой даь
# H0 е p1 = p1 
# H1 e p1 < p2

# спрямо нулевата хипотеза да е less
print(prop.test(x,n, alternative="less", correct=F))

# Тук P.value е 0.03683 значи имаме правото да отвхрълим нулевата хипотеза