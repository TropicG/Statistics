# Задача 34. Генерирайте 500 случайни числа от нормално разпределение с параметри mu = 0, sigma = 1.
# Начертайте хистограма на генерираните числа и на същата картинка добавете графика на плътността на случайна величина X ~ N(mu = 0, sigma = 1).
# Повторете същото с 5000 случайни числа.

# генериране на 500 числа за нормалното разпределение
norm <- rnorm(5000, 0, 1)
# чертаена на графата
hist(norm, probability=T)
# чертаена на графата за дадената точка x
curve(dnorm(x, 0, 1), add=T, lwd=2)