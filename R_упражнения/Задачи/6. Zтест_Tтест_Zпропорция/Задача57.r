# Виж задача 72 от problems_v8.pdf

# Тази задача ще се реши с z-test средно
# Причината голяма бройка елементи с който работим >= 30
# Имаме отклонение и средно

# H0 = 6.7
# H1 != 6.7

H0 <- 6.7
sigma <- 0.12

n <- 45 
H1 <- 6.73

z.obs <- (H1 - H0)/(sigma / sqrt(n))
p.value <- 2*(1 - pnorm(abs(z.obs)))

print(p.value)

# Понеже p.value > 0.5 може да стигнем до заключение че можем да отхвърлим
# нулевата хипотеза 